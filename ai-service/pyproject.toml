[project]
name = "ai-service"
version = "0.1.0"
description = "FastAPI food recognition service"
requires-python = ">=3.10"

[dependencies]
fastapi = "^0.110.0"
uvicorn = "^0.29.0"
pydantic = "^2.7.0"
torch = "^2.4.0"
